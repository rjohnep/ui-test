import PropTypes from 'prop-types';
import React, { {{type}} } from 'react';

export default class {{ properCase name }} extends {{{ type }}} {
  static propTypes = {

  };

  render() {
    return (
      <div>
        Hola {{ properCase name }}
      </div>
    );
  }
}
