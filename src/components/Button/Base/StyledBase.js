import styled from 'styled-components';
import CoreButton from 'core/Button';

const StyledBase = styled(CoreButton)`
  text-transform: uppercase;
  padding: 0 10px;
  line-height: 36px;
  border-radius: 3px;
`;

export default StyledBase;
